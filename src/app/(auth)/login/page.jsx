import React from "react";
import LoginForm from "@/components/custom/forms/auth/LoginForm";

export const metadata = {
  title: "Login | Minar - The One Stop Shopping Experience",
  description: "Login to Minar to discover, compare and shop better.",
};

const LoginPage = () => {
  return <LoginForm />;
};

export default LoginPage;
